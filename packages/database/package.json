{
  "name": "@repo/database",
  "version": "0.0.1",
  "private": true,
  "description": "Database access layer — MongoDB client, repository pattern, tenant-aware queries, indexing, migrations",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/config": "workspace:*",
    "@repo/logger": "workspace:*",
    "@repo/types": "workspace:*",
    "mongoose": "^9.7.0"
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:*",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
