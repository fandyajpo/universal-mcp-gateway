{
  "name": "@repo/connector-sdk",
  "version": "0.0.1",
  "private": true,
  "description": "SDK for building connectors — connector interface, OAuth helpers, webhook handling, sync primitives",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/mcp": "workspace:*",
    "@repo/types": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
