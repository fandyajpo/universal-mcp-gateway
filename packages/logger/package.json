{
  "name": "@repo/logger",
  "version": "0.0.1",
  "private": true,
  "description": "Structured logging — Pino-based, request context, correlation IDs, log levels, transport configuration",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/types": "workspace:*",
    "pino": "^9.5.0",
    "pino-pretty": "^13.0.0",
    "@sentry/core": "^8.0.0"
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:*",
    "typescript": "^6.0.3",
    "vitest": "^3.1.0"
  }
}
