{
  "name": "@repo/types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types — domain models, DTOs, API contracts, enums, utility types",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:*",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
